# Local env / secrets — secrets live OUTSIDE the repo (~/.config/ceo-chat/secrets.env)
# but never let a stray copy get committed.
*.env
.env
.env.*
secrets.env

# Generated audio + narration output (spikes and the broker)
phase0/out/
out/
*.wav
*.pcm

# Node
node_modules/
npm-debug.log*

# TypeScript build artifacts (we run .ts directly on Node >=22; build is typecheck)
dist/
*.tsbuildinfo
